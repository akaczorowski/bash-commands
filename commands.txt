


############### Android signed apk analyze

keytool.exe -list -v -keystore .\notespot-keystore.jks

cd C:\Users\<user>\AppData\Local\Android\Sdk\build-tools\34.0.0

apksigner verify --print-certs my_app.apk

// compare printed certs sha256 if certs are the same



#################### Find apk in folder and print certs

find . -name "*.apk" -exec echo "APK: {}" \; -exec sh -c 'keytool -printcert -jarfile "{}" | grep SHA1' \;
